export const defaultScreenOptions = {
  headerShown: false,
  contentStyle: {
    backgroundColor: '#FFFFFF',
  },
} as const;

export const modalScreenOptions = {
  presentation: 'modal',
  headerShown: true,
} as const;
